n, m=map(int,input().split(" "))
i=0
s = {1:0}
while (i<m):
    x, y=map(int, input().split(" ")
    if (x in s.keys()):
             s[x]++
    else:
             s[x]=1
    if (y in s.keys()):
             s[y]++
    else:
             s[y]=1
    i++
sorted(s.keys())
for k, v in knights.items():
             print(k, v)
             
